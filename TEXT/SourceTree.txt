깃허브 레파스토리 받아올꺼 복사

clone -> 주소붙여넣기 -> 저장할 새폴더 선택 -> clone


Create-> 목적지--경로 -> 파일지정 or 새폴더 -> 파일1개 생성후 커밋 -> 
dev브랜치 만든뒤 파일1개 생성후 커밋 -> feature/function1 브랜치 생성 -> 
dev브랜치로 이동 -> feature/function2 브랜치 생성 -> 
function1 브랜치로 이동 파일1개 생성후 커밋 -> function2 브랜치로 이동 파일1개 생성후 커밋->
dev이동후 파일1개더 생성 -> V0.2 Dev로 커밋

서로 작업하다가 dev V0.2로 올라가버리면 
최근 꼬리되던 베이스로 리베이스 시켜야한다.

베이스가 바뀐 Dev에서 병합

기본충돌이 없으면 fast-forward 커밋이지만 커밋을 확인하기위해 새 커밋 생성

feature브랜치는 작업을 완료한후 병합한뒤 삭제하는게 정상

기준이 되는 브랜치에서 병합할때 시점을 만듬.
에라이 싯팔


=========================================================================
function1브랜치에서 dev브랜치 우클릭후 재배치

dev에서 변경사항있으면 위처럼 재배치(REBASE)를 이용한뒤 병합을 한다.

=========================================
GITFLOW
여러명이 개발할때 용도별로 브랜치를 나눠서 병합하고 나누고 하는 방법을 의미함.

개발은 dev에서 하고 기능이 필요하면 feature브랜치를 만들어서 사용하고 병합이 필요한 시점에서 병합
문제가 생기면 핫픽스를 함.

master브랜치 만든뒤, 깃플로우 사용해서 develop브랜치를 생성하고, 새 파일 만들기

dev브랜치에서 깃플로우 누르면 feature브랜치가 만들어짐.
그런식으로 하다가

dev브랜치에서 완성 text파일 만든뒤 커밋
